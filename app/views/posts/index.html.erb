<h1>Posts#index</h1>

<ul>
  <% @posts.each do |post| %>
  <li>
    <%=  link_to post.title, post %>

  </li>
    <div>
  <p> <%= post.body %></p><p><%= post.title %></p>
</div>
    <% end %>
</ul>

<%= link_to "Create new post", new_post_path %>