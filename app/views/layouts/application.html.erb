<!DOCTYPE html>
<html>
  <head>
    <title>MyApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body style="background-color: #DADADA">
  <div style=" padding: 20px; background-color: burlywood">
    <nav style="text-align: center">
      <% if current_user.present? %>
          <span style="align-self: start">Hi, <%= current_user.nickname %></span>
        <%= link_to "Create new post", new_post_path %>
        <%= link_to "Log out", new_session_path, data: {
          turbo_method: :delete,
          turbo_confirm: "Are you sure?"
        } %>
      <% else %>
        <%= link_to "Home", root_path %>
        <%= link_to "Registration", new_user_path %>
      <% end %>
        <%= link_to 'Login', new_session_path %>
    </nav>



  </div>
  <hr>
    <% if flash[:notice].present? %>
      <p style="border: #730E15; background-color: burlywood; color: #730E15"><%= flash[:notice] %></p>
    <% end %>
    <%= yield %>
  </body>
</html>
